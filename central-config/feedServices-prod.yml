configserver:
  name: JHipster Registry config server
  status: Connected to the JHipster Registry config server!
eureka:
    instance:
        prefer-ip-address: false
        nonSecurePort: 80
        appname: feedServices
        hostname: staging-feed.ap-southeast-1.elasticbeanstalk.com
