configserver:
  name: JHipster Registry config server
  status: Connected to the JHipster Registry config server!
jhipster:
  security:
    authentication:
      jwt:
        secret: my-secret-token-to-change-in-production
eureka:
    instance:
        prefer-ip-address: false
        nonSecurePort: 80
        appname: mediaServices
        hostname: staging-media-mediastep.ap-southeast-1.elasticbeanstalk.com
